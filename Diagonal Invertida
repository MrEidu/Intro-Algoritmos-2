#include <stdio.h>
#include
using namespace std;

int main () {
    int n;
    int Matriz[n][n];
    for (int i = 0; i < n ; i++) {
        for (int j = 0; j < n; j++) {
            if (i + j == n)
                cout << Matriz[i][j];
        }
    }
}   
